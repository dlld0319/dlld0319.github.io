name: deploy

on:
  
  workflow_dispatch

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    strategy:
      matrix:
        node-version: [16.x]

    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}

      - name: npm install, build
        run: |
          # Build and test the project, then
          # deploy to Azure Web App.
          yarn install
          yarn deploy

      - name: Deploy
        uses: JamesIves/github-pages-deploy-action@releases/v3
        with:
          ACCESS_TOKEN: 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
          REPOSITORY_NAME: ld122481669/ld122481669.github.io
          BRANCH: main
          FOLDER: public
